name: Component Versions and ACR Conditional Publish

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: '0 21 * * *'

jobs:
  component-versions:
    name: Print versions and conditionally publish to ACR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build stage and print Go module versions
        run: |
          docker build \
            --progress=plain \
            --target build \
            --tag docker-component-version-test:build \
            .

      - name: Build Alpine image and print versions
        run: |
          docker build \
            --progress=plain \
            --target release-alpine \
            --tag docker-component-version-test:alpine \
            .

      - name: Build Ubuntu image and print versions
        run: |
          docker build \
            --progress=plain \
            --target release-ubuntu \
            --tag docker-component-version-test:ubuntu \
            .

      - name: Validate required secrets for ACR publish
        run: |
          if [ -z "${{ secrets.AZURE_CREDENTIALS }}" ]; then
            echo "AZURE_CREDENTIALS secret is missing"
            exit 1
          fi

          if [ -z "${{ secrets.ACR_NAME }}" ]; then
            echo "ACR_NAME secret is missing"
            exit 1
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Prepare ACR context
        env:
          ACR_NAME: ${{ secrets.ACR_NAME }}
        run: |
          set -euo pipefail
          ACR_NAME_RAW="$ACR_NAME"
          ACR_NAME=$(echo "$ACR_NAME_RAW" | tr '[:upper:]' '[:lower:]' | sed -e 's#^https\?://##' -e 's#/.*$##' -e 's#\.azurecr\.io$##' -e "s/[\"']//g" | xargs)
          ACR_LOGIN_SERVER="$ACR_NAME.azurecr.io"
          ACR_REPOSITORY="docker-component-version-test"
          PUBLISH_TAG="1.0"
          TAG_PREFIX="component-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          CANDIDATE_TAG="${TAG_PREFIX}-candidate"

          az acr login --name "$ACR_NAME"

          EXISTING_DIGEST=$(az acr manifest list-metadata \
            --registry "$ACR_NAME" \
            --name "$ACR_REPOSITORY" \
            --orderby time_desc \
            --query "[?tags!=null && contains(tags, '$PUBLISH_TAG')].digest | [0]" \
            -o tsv 2>/dev/null || true)

          if [ -n "$EXISTING_DIGEST" ] && [ "$EXISTING_DIGEST" != "None" ]; then
            echo "EXISTING_DIGEST=$EXISTING_DIGEST" >> "$GITHUB_ENV"
            echo "Existing digest for $PUBLISH_TAG: $EXISTING_DIGEST"
          else
            echo "EXISTING_DIGEST=" >> "$GITHUB_ENV"
            echo "Existing digest for $PUBLISH_TAG: <none>"
          fi

          echo "ACR_NAME=$ACR_NAME" >> "$GITHUB_ENV"
          echo "ACR_LOGIN_SERVER=$ACR_LOGIN_SERVER" >> "$GITHUB_ENV"
          echo "ACR_REPOSITORY=$ACR_REPOSITORY" >> "$GITHUB_ENV"
          echo "PUBLISH_TAG=$PUBLISH_TAG" >> "$GITHUB_ENV"
          echo "CANDIDATE_TAG=$CANDIDATE_TAG" >> "$GITHUB_ENV"

          echo "Using registry: $ACR_LOGIN_SERVER"
          echo "Using repository: $ACR_REPOSITORY"
          echo "Publish tag: $PUBLISH_TAG"
          echo "Candidate tag: $CANDIDATE_TAG"

      - name: Build current Ubuntu image and push candidate
        run: |
          set -euo pipefail
          docker buildx build \
            --progress=plain \
            --provenance=false \
            --sbom=false \
            --target release-ubuntu \
            --tag "$ACR_LOGIN_SERVER/$ACR_REPOSITORY:$CANDIDATE_TAG" \
            --push \
            .

          CANDIDATE_DIGEST=$(az acr manifest list-metadata \
            --registry "$ACR_NAME" \
            --name "$ACR_REPOSITORY" \
            --orderby time_desc \
            --query "[?tags!=null && contains(tags, '$CANDIDATE_TAG')].digest | [0]" \
            -o tsv)

          if [ -z "$CANDIDATE_DIGEST" ]; then
            echo "Cannot find digest for candidate tag $CANDIDATE_TAG"
            exit 1
          fi

          echo "CANDIDATE_DIGEST=$CANDIDATE_DIGEST" >> "$GITHUB_ENV"
          echo "Current build digest: $CANDIDATE_DIGEST"

      - name: Compare with existing latest digest
        run: |
          set -euo pipefail

          resolve_effective_size() {
            local digest="$1"
            local direct_size
            local media_type
            local child_size

            direct_size=$(az acr manifest list-metadata \
              --registry "$ACR_NAME" \
              --name "$ACR_REPOSITORY" \
              --orderby time_desc \
              --query "[?digest=='$digest'].imageSize | [0]" \
              -o tsv 2>/dev/null || true)

            media_type=$(az acr manifest list-metadata \
              --registry "$ACR_NAME" \
              --name "$ACR_REPOSITORY" \
              --orderby time_desc \
              --query "[?digest=='$digest'].mediaType | [0]" \
              -o tsv 2>/dev/null || true)

            if [ -n "$direct_size" ] && [ "$direct_size" != "None" ] && [ "$direct_size" != "0" ]; then
              echo "$direct_size"
              return 0
            fi

            if [ "$media_type" = "application/vnd.docker.distribution.manifest.list.v2+json" ] || [ "$media_type" = "application/vnd.oci.image.index.v1+json" ]; then
              child_size=$(az acr manifest show \
                --registry "$ACR_NAME" \
                --name "$ACR_REPOSITORY@$digest" \
                --query "manifests[?platform.os=='linux' && platform.architecture=='amd64'].size | [0]" \
                -o tsv 2>/dev/null || true)

              if [ -z "$child_size" ] || [ "$child_size" = "None" ]; then
                child_size=$(az acr manifest show \
                  --registry "$ACR_NAME" \
                  --name "$ACR_REPOSITORY@$digest" \
                  --query "manifests[0].size" \
                  -o tsv 2>/dev/null || true)
              fi

              if [ -n "$child_size" ] && [ "$child_size" != "None" ]; then
                echo "$child_size"
                return 0
              fi
            fi

            echo ""
          }

          EXISTING_IMAGE_SIZE=$(resolve_effective_size "${EXISTING_DIGEST:-}")
          CANDIDATE_IMAGE_SIZE=$(resolve_effective_size "$CANDIDATE_DIGEST")

          if [ -n "$EXISTING_IMAGE_SIZE" ]; then
            echo "EXISTING_IMAGE_SIZE=$EXISTING_IMAGE_SIZE" >> "$GITHUB_ENV"
          else
            echo "EXISTING_IMAGE_SIZE=" >> "$GITHUB_ENV"
          fi

          if [ -n "$CANDIDATE_IMAGE_SIZE" ]; then
            echo "CANDIDATE_IMAGE_SIZE=$CANDIDATE_IMAGE_SIZE" >> "$GITHUB_ENV"
          else
            echo "CANDIDATE_IMAGE_SIZE=" >> "$GITHUB_ENV"
          fi

          {
            echo "## Component Versions ACR Publish"
            echo ""
            echo "- Registry: $ACR_LOGIN_SERVER"
            echo "- Repository: $ACR_REPOSITORY"
            echo "- Publish tag: $PUBLISH_TAG"
            echo "- Existing digest: ${EXISTING_DIGEST:-<none>}"
            echo "- Current build digest: $CANDIDATE_DIGEST"
            echo "- Existing image size: ${EXISTING_IMAGE_SIZE:-<unknown>}"
            echo "- Current build image size: ${CANDIDATE_IMAGE_SIZE:-<unknown>}"
          } >> "$GITHUB_STEP_SUMMARY"

          if [ -n "${EXISTING_DIGEST:-}" ] && [ "$EXISTING_DIGEST" = "$CANDIDATE_DIGEST" ]; then
            echo "Result: identical digest exists, skip publishing."
            echo "- Result: identical digest exists, skip publishing" >> "$GITHUB_STEP_SUMMARY"
            echo "SHOULD_PUBLISH=false" >> "$GITHUB_ENV"
          elif [ -n "${EXISTING_DIGEST:-}" ] && [ -n "${EXISTING_IMAGE_SIZE:-}" ] && [ -n "${CANDIDATE_IMAGE_SIZE:-}" ] && [ "$EXISTING_IMAGE_SIZE" = "$CANDIDATE_IMAGE_SIZE" ]; then
            echo "Result: digest changed but image size unchanged, skip publishing."
            echo "- Result: digest changed but image size unchanged, skip publishing" >> "$GITHUB_STEP_SUMMARY"
            echo "SHOULD_PUBLISH=false" >> "$GITHUB_ENV"
          else
            echo "Result: digest/size indicate change, proceed with publishing."
            echo "- Result: digest/size indicate change, proceed with publishing" >> "$GITHUB_STEP_SUMMARY"
            echo "SHOULD_PUBLISH=true" >> "$GITHUB_ENV"
          fi

      - name: Publish release tag when digest changed
        if: env.SHOULD_PUBLISH == 'true'
        run: |
          set -euo pipefail
          docker buildx imagetools create \
            --tag "$ACR_LOGIN_SERVER/$ACR_REPOSITORY:$PUBLISH_TAG" \
            "$ACR_LOGIN_SERVER/$ACR_REPOSITORY@$CANDIDATE_DIGEST"

          echo "Published tag: $ACR_REPOSITORY:$PUBLISH_TAG -> $CANDIDATE_DIGEST"

          {
            echo "- Published: $ACR_REPOSITORY:$PUBLISH_TAG"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Cleanup temporary tags (best effort)
        if: always()
        run: |
          set +e
          az acr repository untag --name "$ACR_NAME" --image "$ACR_REPOSITORY:$CANDIDATE_TAG"
          exit 0
